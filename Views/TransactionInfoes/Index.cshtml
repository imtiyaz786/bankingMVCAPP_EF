@model IEnumerable<bankingMVCApp_EF.Models.EF.TransactionInfo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TrFromAccount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrToAccount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrAmount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TrFromAccount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrToAccount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrAmount)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TrId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TrId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TrId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
